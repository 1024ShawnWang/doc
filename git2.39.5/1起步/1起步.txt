
#1. git是分布式版本控制系统

#2. git会生成连续的文件快照

3. git分为三个工作阶段: 已修改（程序员对源代码进行升级，自动标记）， 已暂存(对修改文件进行标记，让程序员决定提交哪些改变，它们会一起在commit命令执行时被提交）， 已提交(committed，被正式存储到数据库（git仓库）

4.基本流程
在工作区中修改文件。
将你想要下次提交的更改选择性地暂存，这样只会将更改的部分添加到暂存区。
提交更新，找到暂存区的文件，将快照永久性存储到 Git 目录。

